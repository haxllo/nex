name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]

    runs-on: ${{ matrix.os }}

    env:
      NEX_WINDOWS_RUNTIME_SMOKE: '1'

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm

      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run Vitest gate
        run: ./node_modules/.bin/vitest --run

      - name: Run Rust test gate
        run: cargo test -p nex-cli

      - name: Run Rust perf exact gate
        run: cargo test -p nex-cli --test perf_query_latency_test -- --exact warm_query_p95_under_15ms

      - name: Run runtime smoke harness gate
        run: cargo test -p nex-cli --test windows_runtime_smoke_test
